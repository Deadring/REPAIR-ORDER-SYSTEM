{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd ../api && npm install",
  "outputDirectory": "frontend/build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
